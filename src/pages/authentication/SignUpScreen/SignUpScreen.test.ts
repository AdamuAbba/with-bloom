import { render } from "@testing-library/react";

test("renders a message", () => {
	// const { getByText } = render(<LoginScreen/>);
	// const messageElement = getByText(/Login\sScreen/i);
	// expect(messageElement).toBeInTheDocument();
});
